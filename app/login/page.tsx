"use client";
import{useState}from"react";
export default function Login(){
 const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[showPassword,setShowPassword]=useState(false),[error,setError]=useState(""),[busy,setBusy]=useState(false);
 async function submit(e:React.FormEvent){e.preventDefault();if(busy)return;setBusy(true);setError("");try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:email.trim(),password}),cache:"no-store"});const j=await r.json();if(!r.ok){setError(j.error||"Login failed");return}window.location.replace(j.redirect)}catch{setError("Unable to connect. Please try again.")}finally{setBusy(false)}}
 return <main className="loginPage"><div className="loginGlow"/><section className="loginbox"><div className="loginBrand"><img src="/veg-mantra-logo.svg" alt="Veg Mantra"/><div><b>Veg Mantra</b><span>Mohan Nagar</span></div></div><div className="loginCard"><span className="eyebrow">STAFF ATTENDANCE</span><h1>Welcome back</h1><p className="muted">Sign in as Owner or Manager to continue.</p>{error&&<div className="error">{error}</div>}<form onSubmit={submit}><label>Email</label><input type="email" required autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} disabled={busy} placeholder="you@example.com"/><label>Password</label><div className="passwordField"><input type={showPassword?"text":"password"} required autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} disabled={busy} placeholder="Enter your password"/><button type="button" onClick={()=>setShowPassword(v=>!v)} disabled={busy}>{showPassword?"Hide":"Show"}</button></div><button className="btn loginBtn" disabled={busy}>{busy?"Signing in…":"Sign in"} <span>→</span></button></form><div className="loginFoot"><span>VEG MANTRA</span><span>•</span><span>MOHAN NAGAR</span></div></div></section></main>
}
