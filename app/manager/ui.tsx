"use client";
import{useEffect,useState}from"react";
type Staff={id:string;staffCode:string;name:string;age?:number|null;gender?:string|null;photoData?:string|null};
type AttendanceRow={id:string;staffId:string;date:string;checkIn?:string|null;checkOut?:string|null;status?:string;note?:string|null;virtual?:boolean};
export default function ManagerClient(){
 const[staff,setStaff]=useState<Staff[]>([]),[rows,setRows]=useState<AttendanceRow[]>([]),[message,setMessage]=useState(""),[search,setSearch]=useState(""),[absentStaff,setAbsentStaff]=useState<Staff|null>(null),[reason,setReason]=useState("");
 async function load(){try{const[a,b]=await Promise.all([fetch("/api/staff",{cache:"no-store"}),fetch("/api/attendance",{cache:"no-store"})]);const s=await a.json(),r=await b.json();setStaff(Array.isArray(s)?s:[]);setRows(Array.isArray(r)?r:[])}catch{setMessage("Unable to load attendance. Please try again.")}}
 useEffect(()=>{void load()},[]);
 async function mark(staffId:string,action:"checkin"|"checkout"|"absent",absenceReason=""){setMessage("");try{const r=await fetch("/api/attendance",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({staffId,action,reason:absenceReason})});const j=await r.json();setMessage(r.ok?"Attendance saved successfully.":j.error||"Attendance failed.");if(r.ok){setAbsentStaff(null);setReason("")}await load()}catch{setMessage("Unable to save attendance. Please try again.")}}
 const todayLabel=new Date().toLocaleDateString("en-IN",{timeZone:"Asia/Kolkata"}),filtered=staff.filter(p=>(p.name+" "+p.staffCode).toLowerCase().includes(search.toLowerCase()));
 async function logout(){await fetch("/api/auth/logout",{method:"POST"});location.href="/login"}
 return <div className="managerShell"><header className="managerTop"><div className="managerBrand"><img src="/veg-mantra-logo.svg" alt="Veg Mantra"/><div><b>VEG MANTRA</b><span>MOHAN NAGAR</span></div></div><div className="managerRight"><span><i/> Manager • Cafe network</span><button onClick={logout}>Sign out</button></div></header><main className="managerMain"><section className="managerHero"><div><span className="eyebrow">TODAY • IST</span><h1>Staff attendance</h1><p>Quickly check staff in, check them out, or record an absence.</p></div><div className="dateBadge">▣ {todayLabel}</div></section>{message&&<div className="notice">{message}<button onClick={()=>setMessage("")}>×</button></div>}<section className="managerToolbar"><div><b>Current staff</b><small>{staff.length} active staff member{staff.length===1?"":"s"}</small></div><input placeholder="Search name or Staff ID…" value={search} onChange={e=>setSearch(e.target.value)}/></section>{filtered.length===0?<div className="empty managerEmpty"><b>No staff members found</b><span>Ask the Owner to add staff members.</span></div>:<div className="managerList">{filtered.map(person=>{const todayRows=rows.filter(r=>r.staffId===person.id&&new Date(r.date).toLocaleDateString("en-IN",{timeZone:"Asia/Kolkata"})===todayLabel);const today=todayRows.find(r=>!r.virtual)||todayRows[0];const recorded=Boolean(today&&!today.virtual);return <article className="managerStaff" key={person.id}><div className="managerPerson">{person.photoData?<img src={person.photoData} alt=""/>:<div className="avatar">{person.name[0]}</div>}<div><span className="staffId">{person.staffCode}</span><h3>{person.name}</h3><small>Age {person.age??"—"} • {person.gender??"—"}</small></div></div><div className="managerState">{today?.status==="ABSENT"&&recorded?<><b className="stateAbsent">Absent</b><small>{today.note||"Reason recorded"}</small></>:today?.checkIn?<><b className="statePresent">{today.checkOut?"Completed":"Checked in"}</b><small>{new Date(today.checkIn).toLocaleTimeString("en-IN",{timeZone:"Asia/Kolkata"})}{today.checkOut?" • "+new Date(today.checkOut).toLocaleTimeString("en-IN",{timeZone:"Asia/Kolkata"}):" • Not checked out"}</small></>:<><b>Not recorded</b><small>Waiting for today's attendance</small></>}</div><div className="managerActions">{!recorded&&!today?.checkIn&&<><button className="btn" onClick={()=>void mark(person.id,"checkin")}>✓ Check in</button><button className="btn danger" onClick={()=>{setAbsentStaff(person);setReason("")}}>Mark absent</button></>}{!recorded&&today?.checkIn&&!today?.checkOut&&<button className="btn checkoutBtn" onClick={()=>void mark(person.id,"checkout")}>✓ Check out</button>}{today?.checkOut&&<span className="completeTag">✓ Complete</span>}{today?.status==="ABSENT"&&recorded&&<span className="completeTag absentTag">✓ Absent recorded</span>}</div></article>})}</div>}{absentStaff&&<div className="modalBackdrop"><div className="modal card"><span className="eyebrow">ATTENDANCE</span><h2>Mark {absentStaff.name} absent?</h2><p className="muted">{absentStaff.staffCode} • A reason is required and will be saved for the Owner.</p><textarea value={reason} onChange={e=>setReason(e.target.value)} placeholder="Enter reason for absence…" rows={4}/><div className="actions"><button className="btn secondary" onClick={()=>setAbsentStaff(null)}>Cancel</button><button className="btn danger" disabled={!reason.trim()} onClick={()=>void mark(absentStaff.id,"absent",reason.trim())}>Confirm absent</button></div></div></div>}</main></div>
}
